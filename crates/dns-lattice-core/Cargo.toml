[package]
name = "dns-lattice-core"
version = "0.6.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
categories.workspace = true
keywords.workspace = true
description = "Foundational error and result types shared across the DNS Lattice workspace. No networking-specific types, no OS dependency."
readme = "README.md"

[dependencies]
