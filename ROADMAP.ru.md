# Роадмап DNS Lattice

Роадмап упорядочивает дизайн из `ARCHITECTURE.ru.md`. Это источник фактов о
текущей стадии, на которые ссылаются `index.md`, `AGENTS.md` и профили
агентских ролей репозитория. Обновляйте документ при старте, завершении или
изменении объёма стадии и поддерживайте согласованность с `ROADMAP.md`.

Каждая стадия ниже при старте работ разбивается на один или несколько
ограниченных слоёв реализации; этот файл отслеживает статус только на
уровне стадий, а не отдельных слоёв.

## Обозначения статуса стадии

- `planned` — объём определён здесь, работа ещё не начата.
- `active` — работа ведётся.
- `done` — все ворота верификации задачи пройдены, работа слита.

## Стадия 0.0 — Аудит, роадмап, базовая архитектура

Статус: done (этот документ и `ARCHITECTURE.ru.md` — его результат)

- Инвентаризация текущего bootstrap-состояния репозитория (аудит этой
  стадии).
- Фиксация направления зависимостей и границ API относительно остальных
  крейтов Lattice (см. `ARCHITECTURE.ru.md`).
- Определение целевой структуры модулей, потока данных и не-целей, чтобы у
  стадии 0.1 был дизайн для реализации, а не изобретение объёма по ходу
  слоя.
- Не-цель: нет исходного кода, нет публичного API, нет релиза крейта.

## Стадия 0.1 — Базовая модель

Статус: done

- Модель DNS-сообщений: типы запроса, ответа, записей, нужные движку
  (оценить переиспользование существующего крейта DNS-протокола против
  собственных типов; зафиксировать решение как ADR).
- Тип зонального/доменного матчера (точное совпадение, суффикс, wildcard) с
  детерминированными правилами приоритета.
- Типы политики/конфигурации, потребляемые движком и хуками.
- Детерминированные unit-тесты приоритета матчера и round-trip
  кодирования/декодирования сообщений.
- Не-цель: нет сетевого I/O, нет кэша, нет Fake IP.

## Стадия 0.2 — Резолвер и статический split DNS

Статус: done

- Точка входа резолвера: создание из конфигурации, резолвинг одного
  запроса, остановка.
- Статическая split-DNS маршрутизация: сопоставление запроса с группой
  апстримов через матчер стадии 0.1.
- Кэш ответов в памяти с учётом TTL, включая негативное кэширование.
- Фейковый внутрипроцессный апстрим-бэкенд для детерминированных тестов (в
  обычных тестах нет реальной сети, согласно политике CI репозитория).
- Не-цель: пока нет реального сетевого транспорта, нет динамических хуков,
  нет входящего серверного слушателя (резолвер на этой стадии
  задействуется только внутрипроцессно).

## Стадия 0.3 — Транспортные бэкенды апстримов и серверный слушатель

Статус: done

- Стабилизация трейта бэкенда `upstream` (из `ARCHITECTURE.ru.md`). Готово.
- Апстрим-бэкенды UDP и TCP (базовый уровень, без зависимости от TLS/QUIC). Готово.
- Апстрим-бэкенды DoT и DoH за явными Cargo-фичами. Готово.
- Апстрим-бэкенд DoQ за явной Cargo-фичей, если для всех целевых платформ
  доступна поддерживаемая зависимость QUIC/HTTP-3; иначе зафиксировать
  пробел как ADR и отложить. Готово — `quinn` прошёл проверку; Cargo-фича
  `doq` реализована (`DoqBackend`/`DoqBackendConfig`).
- Fallback/переключение между апстримами внутри группы (см. диаграмму
  потока сбоев в `ARCHITECTURE.ru.md`). Готово.
- Входящий слушатель `server`: сначала UDP и TCP, в соответствии с целью
  крейта — встраиваемым сервером в стиле Kestrel — из `ARCHITECTURE.ru.md`;
  слушатели DoT/DoH/DoQ за теми же Cargo-фичами, что и их апстрим-аналоги.
  Базовый уровень UDP/TCP готов (`Server`/`ServerBuilder`); слушатель DoT
  готов (`ServerBuilder::dot_addr`, Cargo-фича `dot`); слушатель DoQ готов
  (`ServerBuilder::doq_addr`, Cargo-фича `doq`); слушатели DoH готовы:
  TCP `ServerBuilder::doh_addr` поддерживает HTTP/1.1 и HTTP/2, согласуемые
  через ALPN, поверх TLS 1.2/1.3, а QUIC `ServerBuilder::doh3_addr` — HTTP/3
  с ALPN `h3` и TLS 1.3 (Cargo-фича `doh`). Финальная кроссплатформенная
  верификация пройдена.
- Не-цель: нет платформенно-специфичного привилегированного транспорта
  (например, raw-сокетов, привязки к привилегированным портам) — это
  остаётся ответственностью составляющего приложения, как правило через
  `net-lattice`.

## Стадия 0.4 — Пул Fake IP

Статус: active

- Реализованы: детерминированное выделение синтетических адресов IPv4/IPv6
  на домен, обратный поиск (адрес -> домен), явная обработка отсутствия
  результата, LRU-вытеснение для каждого семейства при исчерпании пула,
  TTL/истечение и вызываемое приложением in-memory snapshot/restore живых
  отображений.
- Текущая публичная граница: вызываемый приложением пул `fakeip`, хранящий
  только данные. Он пока не переписывает DNS-ответы, не синтезирует PTR, не
  сохраняет отображения долговременно и не интегрируется с
  `Resolver`/`Server`.
- Оставшаяся работа релиза: согласование API/документации/пакета и
  независимый review.

## Стадия 0.5 — Динамические хуки маршрутизации

Статус: planned

- Стабильный(е) трейт(ы) хуков, позволяющие вызывающему коду (например,
  `flow-lattice`) влиять на маршрутизацию конкретного запроса без
  зависимости `dns-lattice` от него на этапе компиляции.
- Правила композиции и приоритета хуков относительно статических правил
  split-DNS.
- Пример/интеграционный тест, симулирующий сквозной хук на основе политики.

## Стадия 0.6 — Укрепление и валидация платформ

Статус: planned

- Кроссплатформенная CI-матрица (Linux/Windows/macOS), покрывающая каждую
  комбинацию фич бэкендов.
- Fuzz/property-тесты для парсинга сообщений и приоритета матчера.
- Финализация trait-приёмника наблюдаемости; документированные структурные
  события.
- Полная синхронизация документации: rustdoc, README крейта, корневые
  EN/RU документы, CHANGELOG, SECURITY, SUPPORT, CONTRIBUTING.

## Стадия 1.0 — Стабильный публичный API и первый релиз

Статус: planned

- Заморозка публичного API; зафиксировано обязательство по SemVer.
- Проверен `cargo package` для крейта; проверена сборка на docs.rs.
- Первый опубликованный релиз на crates.io.
- `index.md` и этот роадмап обновлены, чтобы отражать опубликованную стадию
  вместо «релиз ещё не публиковался».

## Явно вне объёма роадмапа этого крейта

- Изменение DNS-конфигурации уровня ОС — отслеживается в `net-lattice`.
- Компиляция синтаксиса правил — отслеживается в `flow-lattice`.
- Управление устройствами TUN/TAP и пересылкой пакетов.
- Кроссмодульная композиция и сборка приложения — отслеживается в
  `sdk-lattice`.
